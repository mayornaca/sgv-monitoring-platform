# Production environment settings
APP_ENV=prod
APP_DEBUG=false

# Production SMTP Configuration
# Configurar con los datos reales del proveedor SMTP en producción
# Ejemplos comunes:
# Gmail: MAILER_DSN=gmail+smtp://username:password@default
# SendGrid: MAILER_DSN=sendgrid+smtp://apikey:SG.xxxxx@default
# Mailgun: MAILER_DSN=mailgun+smtp://username:password@default?region=us
# SMTP genérico: MAILER_DSN=smtp://user:password@smtp.example.com:587

# Placeholder para configuración SMTP real - DEBE ser configurado antes del deploy
MAILER_DSN=smtp://usuario:contraseña@smtp.gvops.cl:587

# Mercure production URLs
MERCURE_URL=https://vs.gvops.cl/.well-known/mercure
MERCURE_PUBLIC_URL=https://vs.gvops.cl/.well-known/mercure