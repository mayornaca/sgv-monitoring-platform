<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Sistema de Monitoreo{% endblock %}</title>

    {# Webpack Encore Compiled Assets (includes Bootstrap 5, Font Awesome, and all dependencies) #}
    {{ encore_entry_link_tags('app') }}

    {% block stylesheets %}{% endblock %}

    <style>
        body {
            background-color: #f5f5f5;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        }
        .main-container {
            padding: 2rem 0;
        }
        .card {
            box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
            border: 1px solid rgba(0, 0, 0, 0.125);
        }
        .page-header {
            margin-bottom: 1.5rem;
            padding-bottom: 0.5rem;
            border-bottom: 1px solid #dee2e6;
        }
    </style>
</head>
<body>
    <div class="container-fluid main-container">
        {% block body %}{% endblock %}
    </div>

    {# Webpack Encore Compiled Assets (includes jQuery, Bootstrap 5, and all dependencies) #}
    {{ encore_entry_script_tags('app') }}

    {% block javascripts %}{% endblock %}
</body>
</html>
